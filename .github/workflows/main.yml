name: Main workflow

on: [push]

jobs:
  build:

    runs-on: ${{ matrix.runs-on }}
    strategy:
      matrix:
        runs-on: [ubuntu-latest, windows-latest]

    steps:
    - uses: actions/checkout@v1
    - name: Build the licence on Linux
      run: ./util/build -v
      if: runner.os == 'Linux'
    - name: Build the licence on Windows
      run: .\util\build
      if: runner.os == 'Windows'
